# Multi-Stream Development Rules

This project uses the schwarm plugin for parallel development.
Run `/schwarm:dashboard` for stream overview.
Run `/schwarm:pick <stream>` to pick a task.
Run `/schwarm:sync` to refresh the state dashboard.

See the stream-awareness skill (loaded automatically) for full rules.
